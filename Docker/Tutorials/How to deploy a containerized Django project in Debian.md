# How to deploy a containerized Django project with Apache HTTP Server as reverse proxy in Debian

### Steps

1. Create docker compose file that will pull your image from Docker Hub

2. Create an Apache `.conf` for your container and setup as reverse proxy

3. Enable your site via Apache

4. Start your container using Docker Compose
